2. Write a Program to Check whether a year entered by the user is Leap Year or not.

#include <iostream>
#include<cmath>
using namespace std;

int main()
{

    int year;
    cin>>year;
    if((year%4==0 && year%100!=0)|| ( year%400==0))
    cout<<"The year is leap year"<<endl;
    else
    cout<<"Not leap year"<<endl;
    return 0;
}
